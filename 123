class Anagram
  attr_accessor :word

  def initialize(word)
    @word = word
  end

  def match(word, matched_word)
    matched_word.select do |word| word.char.sort == matched_word.char.sort
  end
end


listen = Anagram.new("listen")
listen.match(%w(enlists google inlets banana))
