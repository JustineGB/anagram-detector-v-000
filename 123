class Anagram
  attr_accessor :word

  def initialize(word)
    @word = word
  end

  def match(word)
    array.to_s.split("").sort
    @word.split("").sort.each {|word| @word == word}
  end
end


listen = Anagram.new("listen")
listen.match(%w(enlists google inlets banana))
