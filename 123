class Anagram
  attr_accessor :word

  def initialize(word)
    @word = word
  end

  def match(word)
  match = []
  @word.to_s.split("").sort.match  {|letters| @word == matches }
  end
end

listen = Anagram.new("listen")
listen.match(%w(enlists google inlets banana))
